
export {
	render, renderFile, createEngine, DefaultRegExp, DefaultHandleVars, DefaultOnNullText
} from './renderer';

import {
	render, renderFile, createEngine, DefaultRegExp, DefaultHandleVars, DefaultOnNullText
} from './renderer';

export default {
	render, renderFile, createEngine, DefaultRegExp, DefaultHandleVars, DefaultOnNullText
}